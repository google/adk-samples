# The model the coordinator and the billing sub-agent both use in a live
# run (`adk run app`, `adk web`, `demo.py --live`). Optional: defaults to
# gemini-3.5-flash when unset. The offline demo (`demo.py`, the default)
# replaces the model with a scripted one and ignores this entirely.
MODEL_NAME=gemini-3.5-flash

# Pick one mode for a live run.

# Option A: Google AI Studio
GOOGLE_GENAI_USE_VERTEXAI=false
GOOGLE_API_KEY=<TODO: update-this-value>

# Option B: Vertex AI
# GOOGLE_GENAI_USE_VERTEXAI=true
# GOOGLE_CLOUD_PROJECT=<TODO: update-this-value>
# GOOGLE_CLOUD_LOCATION=us-central1
